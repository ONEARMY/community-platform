export interface BlockedRouteProps {
    children: React.ReactNode;
    redirectUrl?: string;
    redirectLabel?: string;
}
export declare const BlockedRoute: (props: BlockedRouteProps) => import("react/jsx-runtime").JSX.Element;
