export interface ICharacterCountProps {
    currentSize: number;
    minSize: number;
    maxSize: number;
}
export declare const CharacterCount: ({ currentSize, minSize, maxSize, }: ICharacterCountProps) => import("react/jsx-runtime").JSX.Element;
