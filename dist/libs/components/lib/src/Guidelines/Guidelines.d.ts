import { ReactElement } from 'react';

export interface IProps {
    title: string;
    steps: ReactElement[];
}
export declare const Guidelines: ({ title, steps }: IProps) => import("react/jsx-runtime").JSX.Element;
