context('map', () => {
  beforeEach(() => {
    cy.visit('/map')
  })
  it('should navigate without log', () => {
    // TODO add futher tests
  })

  it('should offer option to moderate pins for admin users', () => {

  });

  it('should only show accepted pins on the map', () => {

  });
})
