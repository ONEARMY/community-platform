import { commonColors } from './colors'

export const commonCards = {
  primary: {
    background: 'white',
    border: `2px solid ${commonColors.black}`,
    borderRadius: 1,
    overflow: 'hidden',
  },
}
