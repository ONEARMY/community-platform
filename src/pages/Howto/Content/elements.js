import styled from 'styled-components'
import { Button } from 'src/components/Button'

export const BackButtonTop = styled(Button)`
  margin: 50px;
`

export const BackButtonBottom = styled(Button)`
  margin: 50px auto;
`
